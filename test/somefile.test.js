var assert = require('assert');
describe('Test Application', function () {
    it('should return -1 when the value is not present', function () {
        const valueToCheck = true;
        assert.strictEqual(valueToCheck, true, 'Good! It works!');
    });
});
